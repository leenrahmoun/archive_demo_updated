DEBUG=1
SECRET_KEY=django-insecure-archive-demo-2026-long-random-secret-key-123456789
ALLOWED_HOSTS=*
CORS_ALLOW_ALL_ORIGINS=1

# Database policy:
# - PostgreSQL is the default and expected database.
# - SQLite is optional local fallback only when USE_SQLITE_FALLBACK=1.
USE_SQLITE_FALLBACK=0

# PostgreSQL (default)
USE_SQLITE_FALLBACK=0
POSTGRES_DB=archive_demo
POSTGRES_USER=postgres
POSTGRES_PASSWORD=leenleen
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

